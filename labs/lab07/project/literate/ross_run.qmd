---
title: "Ross model"
subtitle: "Finite-population repair system"
format:
  html:
    toc: true
execute:
  echo: true
  warning: false
  message: false
---

## Цель

Смоделировать систему с основными машинами, резервом и ремонтными устройствами, а затем сравнить среднее время до падения системы с аналитическим решением.

## Подготовка

```{julia}
using DrWatson
@quickactivate "project"

using CSV
using Plots

include(srcdir("DESModels.jl"))
```

## Параметры модели

```{julia}
p = DESModels.RossParams(; machines = 10, spares = 3, repairers = 1, runs = 200, seed = 150)
```

## Запуск симуляции

```{julia}
results, path = DESModels.simulate_ross(p)
summary = DESModels.summarize_ross(results, p)
path_metrics = DESModels.ross_path_metrics(path, p)

CSV.write(datadir("ross_runs.csv"), results)
CSV.write(datadir("ross_path.csv"), path)
CSV.write(datadir("ross_summary.csv"), summary)
CSV.write(datadir("ross_path_metrics.csv"), path_metrics)
summary
```

## Графики

```{julia}
plt_path = plot(
    path.time,
    path.good_machines,
    xlabel = "Time",
    ylabel = "Good machines",
    label = "good machines",
    title = "Ross model: first simulated path",
    linewidth = 2,
    seriestype = :steppost,
)
savefig(plt_path, plotsdir("ross_good_machines.png"))
plt_path
```

```{julia}
plt_queue = plot(
    path.time,
    path.repair_queue,
    xlabel = "Time",
    ylabel = "Repair queue length",
    label = "repair queue",
    title = "Ross model: repair queue",
    linewidth = 2,
    seriestype = :steppost,
)
savefig(plt_queue, plotsdir("ross_repair_queue.png"))
plt_queue
```

```{julia}
plt_hist = histogram(
    results.crash_time,
    bins = 25,
    xlabel = "Crash time",
    ylabel = "Frequency",
    label = "simulation",
    title = "Ross model: crash time distribution",
)
vline!(plt_hist, [summary.analytic_crash_time[1]], label = "analytic mean", linewidth = 2)
savefig(plt_hist, plotsdir("ross_crash_time_histogram.png"))
plt_hist
```

## Вывод

Модель Росса показывает, как резервирование и ремонт влияют на время до отказа системы. Аналитическое решение используется для проверки среднего времени до падения.
